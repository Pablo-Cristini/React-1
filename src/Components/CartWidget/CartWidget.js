import React from "react";
import LocalMallIcon from '@mui/icons-material/LocalMall';

const CartWidget = () => {
    return (
        <LocalMallIcon color="primary"  sx={{ fontSize: 55 }}/>
    )
}

export default CartWidget;